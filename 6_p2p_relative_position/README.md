# P2P 상대위치 측정 및 거리 계산

P2P로 상대 드론의 위치를 파악하고 거리를 계산하는 에제입니다.

## 설정

1. `src/modules/interface/stabilizer.h`에 파일 넣기
2. `src/modules/src/stabilizer.c`에 파일 넣기
3. `examples/app_peer_to_peer/src/peer_to_peer.c`에 파일 넣기

## 실행

펌웨어 업로드 하여 실행
